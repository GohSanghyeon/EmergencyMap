/*
package com.example.emergencymap

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.bumptech.glide.Glide
import com.bumptech.glide.request.target.GlideDrawableImageViewTarget
import android.widget.ImageView

class EmergencyEducation : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_education_list)

        val rabbit1 = findViewById(R.id.aed1) as ImageView
        val gifImage1 = GlideDrawableImageViewTarget(rabbit1)
        Glide.with(this).load(R.drawable.aed1).into(gifImage1)

        val rabbit2 = findViewById(R.id.aed2) as ImageView
        val gifImage2 = GlideDrawableImageViewTarget(rabbit2)
        Glide.with(this).load(R.drawable.aed2).into(gifImage2)

        val rabbit3 = findViewById(R.id.aed3) as ImageView
        val gifImage3 = GlideDrawableImageViewTarget(rabbit3)
        Glide.with(this).load(R.drawable.aed3).into(gifImage3)

    }
}
*/
